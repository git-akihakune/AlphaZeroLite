# List the start up tasks. Learn more: https://www.gitpod.io/docs/configure/workspaces/tasks
tasks:
  - name: Environment set up
    init: pip install -U pip
    command: pip install . && pip install .[dev]